<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape
                android:innerRadiusRatio="2.5"
                android:thicknessRatio="10"
                android:shape="ring"
                android:useLevel="false"
                android:type="sweep">
                <gradient
                    android:centerColor="?attr/Grey"
                    android:endColor="?attr/Grey"
                    android:startColor="?attr/Grey"
                    android:type="sweep"/>
            </shape>
        </rotate>

    </item>
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape
                android:innerRadiusRatio="2.5"
                android:thicknessRatio="10"
                android:shape="ring"
                android:angle="0"
                android:type="sweep">
                <gradient
                android:centerColor="?attr/Black"
                android:endColor="?attr/Black"
                android:startColor="?attr/Black"
                android:type="sweep"/>
            </shape>
        </rotate>
    </item>
</layer-list>